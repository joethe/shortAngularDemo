
angular.module("services", []);        // module to contain services controllers
angular.module("controllers", []);     // module to contain general purpose controllers
angular.module("main", ["services"]);  // main module for our todoList app
